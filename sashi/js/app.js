// Main application entry point for Sashi D&D Character Sheet
import { initUI } from './ui.js';

// Initialize the UI when the DOM is loaded
document.addEventListener('DOMContentLoaded', () => {
  initUI();
});